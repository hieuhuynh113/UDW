﻿@if (TempData.ContainsKey("message"))
{
    WebApplication.Library.XMessage message = (WebApplication.Library.XMessage)TempData["message"];
    <div class="alert alert-@message.TypeMsg">
        @message.Msg
    </div>
}